name: .NET

on:
  push:
    branches: [ mac-linux-cli ]
    paths-ignore:
      - '**/**.md'
  pull_request:
    branches: [ mac-linux-cli ]
    paths-ignore:
      - '**/**.md'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 6.0.x
        include-prerelease: true
    - name: Restore dependencies
      run: |
        cd src
        dotnet restore
    - name: Build
      run: |
        cd src
        dotnet build --no-restore
